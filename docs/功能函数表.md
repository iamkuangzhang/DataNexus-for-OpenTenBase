# DataNexus for OpenTenBase - 功能对象完整清单

> **统计总览**: 8个插件模块，**292** 个功能对象    
> **版本**: 1.0.0

---

## 📊 模块统计汇总

| 模块 | Schema | 类型 | 函数 | 聚合 | 视图 | 类型 | 表 | 合计 |
|------|--------|------|------|------|------|------|-----|------|
| TimescaleDB 兼容层 | `otb_ts`, `timescaledb` | 适配 | 93 | 19 | 14 | 2 | 6 | **134** |
| Apache AGE 兼容层 | `otb_age`, `ag_catalog` | 适配 | 30 | 0 | 2 | 0 | 5 | **37** |
| 全文检索 | `otb_fulltext` | 适配 | 35 | 0 | 0 | 0 | 6 | **41** |
| 路网分析 | `otb_routing` | 适配 | 12 | 0 | 0 | 0 | 2 | **14** |
| 调度管理 | `otb_scheduler` | 适配 | 28 | 0 | 4 | 1 | 7 | **40** |
| 时序分析算法库 | `otb_analytics` | 原创 | 1 | 10 | 0 | 0 | 0 | **11** |
| 数据健康诊断 | `otb_health` | 原创 | 9 | 0 | 0 | 0 | 0 | **9** |
| 数据快照系统 | `otb_snapshot` | 原创 | 5 | 0 | 0 | 0 | 1 | **6** |
| **合计** | - | - | **213** | **29** | **20** | **3** | **27** | **292** |

> **说明**: 以上统计不包含内部状态转换函数和辅助函数，仅统计对外公开的API接口。

---

## 1. TimescaleDB 兼容层 (otb_ts / timescaledb)

### 1.1 核心时序函数 (C语言高性能实现)

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `time_bucket` | 函数 | `(interval, timestamptz, timestamptz)` | timestamptz | 时间分桶（16x性能提升） | C |
| 2 | `time_bucket` | 函数 | `(interval, timestamptz)` | timestamptz | 时间分桶（2参数版本） | SQL |
| 3 | `time_bucket` | 函数 | `(interval, timestamp, timestamp)` | timestamp | 时间分桶（无时区） | C |
| 4 | `time_bucket` | 函数 | `(interval, timestamp)` | timestamp | 时间分桶（2参数，无时区） | SQL |
| 5 | `time_bucket_c` | 函数 | `(interval, timestamptz, timestamptz)` | timestamptz | time_bucket别名（性能对比测试用） | SQL |
| 6 | `time_bucket_c` | 函数 | `(interval, timestamptz)` | timestamptz | time_bucket别名（2参数版本） | SQL |
| 7 | `time_bucket_gapfill` | 函数 | `(interval, timestamptz, timestamptz)` | timestamptz | 时间分桶+缺口填充 | C |
| 8 | `time_bucket_gapfill` | 函数 | `(interval, timestamptz)` | timestamptz | 缺口填充（2参数） | SQL |
| 9 | `time_bucket_gapfill` | 函数 | `(interval, timestamp, timestamp)` | timestamp | 缺口填充（无时区） | C |
| 10 | `time_bucket_gapfill` | 函数 | `(interval, timestamp)` | timestamp | 缺口填充（2参数，无时区） | SQL |
| 11 | `time_bucket_epoch` | 函数 | `(bigint, bigint)` | bigint | Unix时间戳分桶 | SQL |
| 12 | `time_bucket_epoch_ms` | 函数 | `(bigint, bigint)` | bigint | 毫秒时间戳分桶 | SQL |
| 13 | `interpolate` | 函数 | `(float8, int8, float8, int8, int8)` | float8 | 线性插值 | C |
| 14 | `interpolate_linear` | 函数 | `(numeric, numeric, timestamp, timestamp, timestamp)` | numeric | 线性插值（TIMESTAMP版） | SQL |
| 15 | `interpolate_linear` | 函数 | `(numeric, numeric, timestamptz, timestamptz, timestamptz)` | numeric | 线性插值（TIMESTAMPTZ版） | SQL |
| 16 | `histogram_c` | 函数 | `(float8, int, float8, float8)` | int | 直方图单值bucket计算 | C |
| 17 | `compression_ratio` | 函数 | `(int8, int8)` | float8 | 压缩率计算 | C |
| 18 | `compress_chunk_data` | 函数 | `(text, text)` | boolean | Chunk批量压缩 | C |
| 19 | `delta_compress` | 函数 | `(int8, bytea)` | bytea | Delta-of-Delta压缩 | C |
| 20 | `gorilla_compress` | 函数 | `(float8, bytea)` | bytea | Gorilla浮点压缩 | C |
| 21 | `approx_percentile` | 函数 | `(float8, float8)` | float8 | 近似百分位数 | SQL |

### 1.2 聚合函数 (C语言高性能实现)

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `otb_ts.first` | 聚合 | `(numeric, timestamptz)` | numeric | 按时间取第一个值（3-5x提升） | C |
| 2 | `otb_ts.last` | 聚合 | `(numeric, timestamptz)` | numeric | 按时间取最后一个值 | C |
| 3 | `otb_ts.first` | 聚合 | `(float8, timestamptz)` | float8 | first (double precision) | C |
| 4 | `otb_ts.last` | 聚合 | `(float8, timestamptz)` | float8 | last (double precision) | C |
| 5 | `otb_ts.first` | 聚合 | `(text, timestamp)` | text | first (文本+TIMESTAMP) | SQL |
| 6 | `otb_ts.last` | 聚合 | `(text, timestamp)` | text | last (文本+TIMESTAMP) | SQL |
| 7 | `otb_ts.first` | 聚合 | `(text, timestamptz)` | text | first (文本+TIMESTAMPTZ) | SQL |
| 8 | `otb_ts.last` | 聚合 | `(text, timestamptz)` | text | last (文本+TIMESTAMPTZ) | SQL |
| 9 | `first_c` | 聚合 | `(numeric, timestamptz)` | numeric | first别名（性能测试用） | C |
| 10 | `last_c` | 聚合 | `(numeric, timestamptz)` | numeric | last别名（性能测试用） | C |
| 11 | `first_c` | 聚合 | `(float8, timestamptz)` | float8 | first别名 (double precision) | C |
| 12 | `last_c` | 聚合 | `(float8, timestamptz)` | float8 | last别名 (double precision) | C |
| 13 | `histogram` | 聚合 | `(float8, int, float8, float8)` | text | 直方图分布统计 | C |
| 14 | `locf` | 聚合 | `(float8)` | float8 | Last Observation Carried Forward | C |
| 15 | `locf` | 聚合 | `(numeric)` | numeric | LOCF (numeric) | C |
| 16 | `time_weight` | 聚合 | `(float8, timestamptz)` | float8 | 时间加权平均 | C |
| 17 | `counter_agg` | 聚合 | `(float8)` | float8 | 计数器聚合（处理重置） | C |
| 18 | `gauge_agg` | 聚合 | `(float8)` | gauge_summary | 仪表盘聚合 | C |
| 19 | `stats_agg` | 聚合 | `(float8)` | stats_summary | 完整统计聚合 | C |

### 1.3 Hypertable 管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_hypertable` | 函数 | `(regclass, text, interval, name, int)` | table | 创建Hypertable | SQL |
| 2 | `show_chunks` | 函数 | `(regclass)` | table | 显示所有chunks | SQL |
| 3 | `show_chunks` | 函数 | `(text, interval, interval)` | table | 显示chunks（按时间过滤） | SQL |
| 4 | `drop_chunks` | 函数 | `(regclass, interval)` | setof text | 删除旧chunks | SQL |
| 5 | `drop_chunks` | 函数 | `(text, interval, boolean)` | table | 删除chunks（详细返回） | SQL |
| 6 | `ensure_chunks` | 函数 | `(regclass, timestamp, timestamp)` | integer | 预创建chunks | SQL |
| 7 | `add_dimension` | 函数 | `(regclass, name, integer, interval, regproc, boolean)` | table | 添加分区维度 | SQL |
| 8 | `set_chunk_time_interval` | 函数 | `(regclass, interval, name)` | void | 修改chunk间隔 | SQL |
| 9 | `compress_chunk` | 函数 | `(regclass)` | boolean | 压缩chunk | SQL |
| 10 | `decompress_chunk` | 函数 | `(regclass)` | boolean | 解压chunk | SQL |
| 11 | `_create_chunk` | 函数 | `(int, text, text, text, timestamp, timestamp)` | boolean | 内部：创建单个chunk | SQL |
| 12 | `hypertable_size` | 函数 | `(regclass)` | bigint | 获取Hypertable大小 | SQL |
| 13 | `hypertable_detailed_size` | 函数 | `(regclass)` | table | 获取详细大小 | SQL |
| 14 | `chunks_detailed_size` | 函数 | `(regclass)` | table | 获取chunks大小 | SQL |

### 1.4 策略管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `add_retention_policy` | 函数 | `(regclass, interval)` | integer | 添加保留策略 | SQL |
| 2 | `remove_retention_policy` | 函数 | `(regclass)` | boolean | 移除保留策略 | SQL |
| 3 | `add_compression_policy` | 函数 | `(regclass, interval, boolean)` | integer | 添加压缩策略 | SQL |
| 4 | `remove_compression_policy` | 函数 | `(regclass, boolean)` | boolean | 移除压缩策略 | SQL |
| 5 | `add_refresh_policy` | 函数 | `(text, interval, interval, interval)` | integer | 添加刷新策略 | SQL |
| 6 | `alter_job` | 函数 | `(integer, interval, interval, integer, interval, boolean, jsonb, timestamptz, boolean)` | table | 修改后台任务 | SQL |
| 7 | `remove_continuous_aggregate_policy` | 函数 | `(regclass, boolean)` | void | 移除连续聚合策略 | SQL |
| 8 | `maintain` | 函数 | `()` | text | 执行维护任务 | SQL |
| 9 | `apply_retention_policies` | 函数 | `()` | integer | 应用保留策略 | SQL |
| 10 | `show_policies` | 函数 | `(regclass)` | table | 显示所有策略 | SQL |

### 1.5 连续聚合函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_continuous_aggregate` | 函数 | `(text, text, boolean)` | boolean | 创建连续聚合 | C |
| 2 | `refresh_continuous_aggregate` | 函数 | `(text, timestamptz, timestamptz)` | int8 | 刷新连续聚合 | C |
| 3 | `auto_refresh_continuous_aggregates` | 函数 | `()` | integer | 自动刷新所有连续聚合 | C |
| 4 | `drop_continuous_aggregate` | 函数 | `(text, boolean)` | boolean | 删除连续聚合 | C |

### 1.6 诊断与工具函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `version` | 函数 | `()` | text | 获取版本信息 | SQL |
| 2 | `version_info` | 函数 | `()` | table | 获取详细版本 | SQL |
| 3 | `get_available_functions` | 函数 | `()` | table | 获取所有函数列表 | SQL |
| 4 | `show_functions` | 函数 | `()` | table | 显示函数（别名） | SQL |
| 5 | `explain_pruning` | 函数 | `(regclass, timestamp, timestamp)` | table | 分区裁剪分析 | SQL |
| 6 | `verify_indexes` | 函数 | `(regclass)` | table | 验证索引完整性 | SQL |
| 7 | `recommend_distribution` | 函数 | `(regclass, name)` | text | 分布策略推荐 | SQL |
| 8 | `optimize_partition_settings` | 函数 | `()` | text | 优化分区设置 | SQL |
| 9 | `enable_auto_chunk_creation` | 函数 | `(regclass)` | boolean | 启用自动分区 | SQL |
| 10 | `downsample` | 函数 | `(regclass, text, interval, text, text)` | bigint | 数据降采样 | SQL |
| 11 | `export_to_json` | 函数 | `(regclass, text, timestamp, timestamp, int)` | json | JSON导出 | SQL |
| 12 | `analyze_data_temperature` | 函数 | `(regclass, interval)` | table | 数据温度分析（按间隔） | SQL |
| 13 | `analyze_data_temperature` | 函数 | `(regclass, int)` | table | 数据温度分析（按天数） | SQL |
| 14 | `reorder_chunk` | 函数 | `(regclass, name)` | boolean | 重排chunk数据 | SQL |

### 1.7 系统视图

| 序号 | 名称 | 类型 | Schema | 功能说明 |
|------|------|------|--------|----------|
| 1 | `hypertables` | 视图 | timescaledb | Hypertable信息 |
| 2 | `chunks` | 视图 | timescaledb | Chunk详细信息 |
| 3 | `dimensions` | 视图 | timescaledb | 分区维度信息 |
| 4 | `compression_settings` | 视图 | timescaledb | 压缩配置 |
| 5 | `jobs` | 视图 | timescaledb | 后台任务信息 |
| 6 | `continuous_aggregates` | 视图 | timescaledb | 连续聚合信息 |
| 7 | `maintenance_log` | 视图 | timescaledb | 维护日志 |
| 8 | `hypertables` | 视图 | timescaledb_information | 系统信息视图 |
| 9 | `chunks` | 视图 | timescaledb_information | 系统信息视图 |
| 10 | `dimensions` | 视图 | timescaledb_information | 系统信息视图 |
| 11 | `jobs` | 视图 | timescaledb_information | 系统信息视图 |
| 12 | `compression_settings` | 视图 | timescaledb_information | 系统信息视图 |
| 13 | `hypertable_stats` | 视图 | timescaledb_information | 统计摘要 |
| 14 | `continuous_aggregates` | 视图 | timescaledb_information | 连续聚合信息 |

### 1.8 自定义类型

| 序号 | 名称 | 类型 | 字段 | 功能说明 |
|------|------|------|------|----------|
| 1 | `gauge_summary` | 复合类型 | min, max, avg, sum, stddev | gauge_agg返回类型 |
| 2 | `stats_summary` | 复合类型 | count, min, max, avg, sum, stddev | stats_agg返回类型 |

### 1.9 元数据表

| 序号 | 名称 | Schema | 功能说明 |
|------|------|--------|----------|
| 1 | `hypertables` | otb_ts | Hypertable注册表 |
| 2 | `chunks` | otb_ts | Chunk注册表 |
| 3 | `policies` | otb_ts | 策略配置表 |
| 4 | `maintenance_log` | otb_ts | 维护日志表 |
| 5 | `metadata` | otb_ts | 元数据配置表 |
| 6 | `continuous_aggregates` | otb_ts | 连续聚合元数据 |

### 1.10 TimescaleDB兼容层包装函数 (timescaledb schema)

> 以下函数位于 `timescaledb` schema，提供与原生TimescaleDB一致的API接口，内部调用 `otb_ts` 对应函数。

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 |
|------|------|------|------|--------|----------|
| 1 | `timescaledb.create_hypertable` | 函数 | `(regclass, text, interval, name, int)` | table | 创建Hypertable（兼容接口） |
| 2 | `timescaledb.add_retention_policy` | 函数 | `(regclass, interval)` | integer | 添加保留策略（兼容接口） |
| 3 | `timescaledb.remove_retention_policy` | 函数 | `(regclass)` | boolean | 移除保留策略（兼容接口） |
| 4 | `timescaledb.show_chunks` | 函数 | `(regclass)` | table | 显示chunks（兼容接口） |
| 5 | `timescaledb.show_chunks` | 函数 | `(text, interval, interval)` | table | 显示chunks（带过滤） |
| 6 | `timescaledb.drop_chunks` | 函数 | `(regclass, interval)` | setof text | 删除chunks（兼容接口） |
| 7 | `timescaledb.drop_chunks` | 函数 | `(text, interval, boolean)` | table | 删除chunks（详细返回） |
| 8 | `timescaledb.ensure_chunks` | 函数 | `(regclass, timestamp, timestamp)` | integer | 预创建chunks |
| 9 | `timescaledb.time_bucket` | 函数 | `(interval, timestamptz)` | timestamptz | 时间分桶（兼容接口） |
| 10 | `timescaledb.time_bucket` | 函数 | `(interval, timestamp)` | timestamp | 时间分桶（无时区） |
| 11 | `timescaledb.time_bucket` | 函数 | `(bigint, bigint)` | bigint | Unix时间戳分桶 |
| 12 | `timescaledb.time_bucket_gapfill` | 函数 | `(interval, timestamptz)` | timestamptz | 缺口填充（兼容接口） |
| 13 | `timescaledb.interpolate` | 函数 | `(anyelement, record, record, record)` | anyelement | 线性插值（兼容接口） |
| 14 | `timescaledb.first` | 函数 | `(numeric, timestamptz)` | numeric | 取第一个值（兼容接口） |
| 15 | `timescaledb.last` | 函数 | `(numeric, timestamptz)` | numeric | 取最后一个值（兼容接口） |
| 16 | `timescaledb.first` | 函数 | `(float8, timestamptz)` | float8 | 取第一个值（double precision） |
| 17 | `timescaledb.last` | 函数 | `(float8, timestamptz)` | float8 | 取最后一个值（double precision） |
| 18 | `timescaledb.add_compression_policy` | 函数 | `(regclass, interval, boolean)` | integer | 添加压缩策略（兼容接口） |
| 19 | `timescaledb.remove_compression_policy` | 函数 | `(regclass)` | boolean | 移除压缩策略（兼容接口） |
| 20 | `timescaledb.reorder_chunk` | 函数 | `(regclass, name)` | void | 重排chunk（兼容接口） |
| 21 | `timescaledb.add_continuous_aggregate_policy` | 函数 | `(text, interval, interval, interval)` | integer | 添加连续聚合策略 |
| 22 | `timescaledb.refresh_continuous_aggregate` | 函数 | `(text, timestamptz, timestamptz)` | void | 刷新连续聚合 |
| 23 | `timescaledb.enable_auto_chunk_creation` | 函数 | `(regclass)` | boolean | 启用自动分区 |
| 24 | `timescaledb.version` | 函数 | `()` | text | 版本信息 |
| 25 | `timescaledb.show_functions` | 函数 | `()` | table | 显示函数列表 |
| 26 | `timescaledb.hypertable_size` | 函数 | `(regclass)` | bigint | 获取hypertable大小 |
| 27 | `timescaledb.hypertable_detailed_size` | 函数 | `(regclass)` | table | 获取详细大小 |
| 28 | `timescaledb.chunks_detailed_size` | 函数 | `(regclass)` | table | 获取chunks大小 |

---

## 2. Apache AGE 兼容层 (otb_age)

### 2.1 图管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_graph` | 函数 | `(text, text)` | table | 创建图 | SQL |
| 2 | `drop_graph` | 函数 | `(text, boolean)` | boolean | 删除图（级联） | SQL |
| 3 | `list_graphs` | 函数 | `()` | table | 列出所有图 | SQL |

### 2.2 标签管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_vlabel` | 函数 | `(text, text)` | integer | 创建顶点标签 | SQL |
| 2 | `create_elabel` | 函数 | `(text, text)` | integer | 创建边标签 | SQL |
| 3 | `list_vlabels` | 函数 | `(text)` | table | 列出顶点标签 | SQL |
| 4 | `list_elabels` | 函数 | `(text)` | table | 列出边标签 | SQL |

### 2.3 顶点/边操作函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `add_vertex` | 函数 | `(text, text, jsonb)` | bigint | 添加顶点 | SQL |
| 2 | `add_vertices` | 函数 | `(text, text, jsonb[])` | integer | 批量添加顶点 | SQL |
| 3 | `add_edge` | 函数 | `(text, bigint, bigint, text, jsonb)` | bigint | 添加边 | SQL |
| 4 | `delete_vertex` | 函数 | `(bigint, boolean)` | boolean | 删除顶点 | SQL |
| 5 | `delete_edge` | 函数 | `(bigint)` | boolean | 删除边 | SQL |
| 6 | `set_vertex_property` | 函数 | `(bigint, text, jsonb)` | boolean | 更新顶点属性 | SQL |
| 7 | `set_edge_property` | 函数 | `(bigint, text, jsonb)` | boolean | 更新边属性 | SQL |

### 2.4 图查询函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `get_vertex` | 函数 | `(bigint)` | table | 获取顶点信息 | SQL |
| 2 | `match_vertices` | 函数 | `(text, text, jsonb)` | table | 按标签/属性查询顶点 | SQL |
| 3 | `match_edges` | 函数 | `(text, text, jsonb)` | table | 按标签/属性查询边 | SQL |
| 4 | `get_neighbors` | 函数 | `(bigint, text, text)` | table | 获取邻居顶点 | SQL |
| 5 | `out_degree` | 函数 | `(bigint, text)` | integer | 获取出度 | SQL |
| 6 | `in_degree` | 函数 | `(bigint, text)` | integer | 获取入度 | SQL |

### 2.5 图算法函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `bfs` | 函数 | `(text, bigint, integer, text)` | table | BFS广度优先遍历 | SQL |
| 2 | `shortest_path` | 函数 | `(text, bigint, bigint, integer, text)` | table | 最短路径（Dijkstra） | SQL |
| 3 | `all_paths` | 函数 | `(text, bigint, bigint, integer, integer)` | table | 所有路径 | SQL |
| 4 | `connected_components` | 函数 | `(text)` | table | 连通分量分析 | SQL |

### 2.6 统计与Cypher函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `graph_stats` | 函数 | `(text)` | table | 图统计信息 | SQL |
| 2 | `degree_distribution` | 函数 | `(text)` | table | 度分布统计 | SQL |
| 3 | `cypher` | 函数 | `(text, text)` | setof jsonb | Cypher查询执行器 | SQL |
| 4 | `version` | 函数 | `()` | text | 版本信息 | SQL |

### 2.7 AGE兼容视图

| 序号 | 名称 | 类型 | Schema | 功能说明 |
|------|------|------|--------|----------|
| 1 | `ag_graph` | 视图 | ag_catalog | 图定义视图 |
| 2 | `ag_label` | 视图 | ag_catalog | 标签定义视图 |

### 2.8 元数据表

| 序号 | 名称 | Schema | 功能说明 |
|------|------|--------|----------|
| 1 | `graphs` | otb_age | 图定义表 |
| 2 | `vertex_labels` | otb_age | 顶点标签表 |
| 3 | `edge_labels` | otb_age | 边标签表 |
| 4 | `vertices` | otb_age | 顶点数据表 |
| 5 | `edges` | otb_age | 边数据表 |

### 2.9 Public别名函数

> 以下函数位于 `public` schema，提供便捷的访问接口。

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 |
|------|------|------|------|--------|----------|
| 1 | `public.create_graph` | 函数 | `(text)` | table | 创建图（简化接口） |
| 2 | `public.drop_graph` | 函数 | `(text, boolean)` | boolean | 删除图（简化接口） |

---

## 3. 全文检索 (otb_fulltext)

### 3.1 分词函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `tokenize` | 函数 | `(text, text)` | text[] | 中英文分词 | SQL |
| 2 | `tokenize_chinese` | 函数 | `(text)` | text[] | 中文字符分词 | SQL |
| 3 | `tokenize_with_pos` | 函数 | `(text, text)` | table | 带词性标注分词 | SQL |
| 4 | `remove_stopwords` | 函数 | `(text[], text)` | text[] | 移除停用词 | SQL |
| 5 | `ngram` | 函数 | `(text, integer)` | text[] | N-gram分词 | SQL |

### 3.2 全文搜索函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `to_tsvector_zh` | 函数 | `(text, text)` | tsvector | 转换为tsvector（中文） | SQL |
| 2 | `to_tsquery_zh` | 函数 | `(text, text)` | tsquery | 转换为tsquery（中文） | SQL |
| 3 | `to_tsvector` | 函数 | `(text, text)` | tsvector | 兼容PG标准函数 | SQL |
| 4 | `match` | 函数 | `(text, text)` | boolean | 全文匹配 | SQL |
| 5 | `rank` | 函数 | `(text, text)` | real | 相关性分数 | SQL |
| 6 | `rank_cd` | 函数 | `(text, text, integer)` | real | 带位置的相关性分数 | SQL |
| 7 | `search_text` | 函数 | `(text, text, text, text, integer)` | table | 在表中搜索文本 | SQL |
| 8 | `similarity_search` | 函数 | `(text, text, text, real, integer)` | table | 相似度搜索 | SQL |
| 9 | `ngram_match` | 函数 | `(text, text, integer, real)` | boolean | N-gram匹配 | SQL |

### 3.3 高亮显示函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `highlight` | 函数 | `(text, text, text, text, integer, integer)` | text | 高亮匹配文本 | SQL |
| 2 | `highlight_text` | 函数 | `(text, text, text, text, text)` | text | 高亮（中文优化） | SQL |
| 3 | `snippet` | 函数 | `(text, text, integer)` | text | 生成摘要 | SQL |

### 3.4 词库管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `add_word` | 函数 | `(text, text, real)` | boolean | 添加自定义词 | SQL |
| 2 | `add_words` | 函数 | `(text[])` | integer | 批量添加自定义词 | SQL |
| 3 | `add_stopword` | 函数 | `(text, text)` | boolean | 添加停用词 | SQL |
| 4 | `add_synonym` | 函数 | `(text, text, text)` | boolean | 添加同义词 | SQL |
| 5 | `get_synonyms` | 函数 | `(text, text)` | text[] | 获取同义词 | SQL |

### 3.5 搜索增强函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `fuzzy_search` | 函数 | `(text, text, real)` | boolean | 模糊搜索 | SQL |
| 2 | `text_similarity` | 函数 | `(text, text)` | real | 文本相似度 | SQL |
| 3 | `prefix_search` | 函数 | `(text, text)` | boolean | 前缀搜索 | SQL |
| 4 | `suggest` | 函数 | `(text, integer)` | table | 搜索建议 | SQL |
| 5 | `log_search` | 函数 | `(text, integer)` | void | 记录搜索历史 | SQL |
| 6 | `hot_searches` | 函数 | `(interval, integer)` | table | 热门搜索 | SQL |

### 3.6 索引管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_index` | 函数 | `(text, text, text, text)` | boolean | 创建全文索引 | SQL |
| 2 | `drop_index` | 函数 | `(text, text)` | boolean | 删除全文索引 | SQL |
| 3 | `list_indexes` | 函数 | `()` | table | 列出所有全文索引 | SQL |

### 3.7 统计函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `word_frequency` | 函数 | `(text)` | table | 词频统计 | SQL |
| 2 | `text_stats` | 函数 | `(text)` | table | 文本统计 | SQL |
| 3 | `version` | 函数 | `()` | text | 版本信息 | SQL |

### 3.8 元数据表

| 序号 | 名称 | Schema | 功能说明 |
|------|------|--------|----------|
| 1 | `dictionaries` | otb_fulltext | 分词字典表 |
| 2 | `stopwords` | otb_fulltext | 停用词表 |
| 3 | `synonyms` | otb_fulltext | 同义词表 |
| 4 | `custom_words` | otb_fulltext | 自定义词库表 |
| 5 | `ft_indexes` | otb_fulltext | 全文索引元数据 |
| 6 | `search_history` | otb_fulltext | 搜索历史表 |

### 3.8 Public别名函数

> 以下函数位于 `public` schema，提供便捷的访问接口。

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 |
|------|------|------|------|--------|----------|
| 1 | `public.ft_tokenize` | 函数 | `(text)` | text[] | 分词（简化接口） |
| 2 | `public.ft_match` | 函数 | `(text, text)` | boolean | 全文匹配（简化接口） |
| 3 | `public.ft_rank` | 函数 | `(text, text)` | float4 | 相关度排名（简化接口） |
| 4 | `public.ft_highlight` | 函数 | `(text, text)` | text | 高亮显示（简化接口） |

---

## 4. 路网分析 (otb_routing)

### 4.1 路径计算函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `dijkstra` | 函数 | `(text, bigint, bigint, boolean)` | table | Dijkstra最短路径 | SQL |
| 2 | `driving_distance` | 函数 | `(text, bigint, float8, boolean)` | table | 驾驶距离/等时圈 | SQL |

### 4.2 路网管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_road_network` | 函数 | `(text, text)` | void | 创建路网表 | SQL |
| 2 | `create_topology` | 函数 | `(text, float8, text)` | text | 创建拓扑（提取节点） | SQL |
| 3 | `analyze_graph` | 函数 | `(text, text)` | table | 分析路网 | SQL |

### 4.3 辅助函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `find_nearest_node` | 函数 | `(text, float8, float8, text)` | bigint | 找到最近节点 | SQL |
| 2 | `distance` | 函数 | `(float8, float8, float8, float8)` | float8 | 两点间距离 | SQL |
| 3 | `path_length` | 函数 | `(text, bigint[])` | float8 | 计算路径总长度 | SQL |
| 4 | `network_stats` | 函数 | `(text, text)` | table | 路网统计 | SQL |
| 5 | `version` | 函数 | `()` | text | 版本信息 | SQL |

### 4.4 模板表

| 序号 | 名称 | Schema | 功能说明 |
|------|------|--------|----------|
| 1 | `edges_template` | otb_routing | 路网边表模板 |
| 2 | `vertices_template` | otb_routing | 路网节点表模板 |

---

## 5. 调度管理 (otb_scheduler)

### 5.1 任务调度函数 (pg_cron兼容)

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `schedule` | 函数 | `(text, text)` | bigint | 创建定时任务 | SQL |
| 2 | `schedule` | 函数 | `(text, text, text)` | bigint | 创建定时任务（带名称） | SQL |
| 3 | `unschedule` | 函数 | `(bigint)` | boolean | 删除任务（按ID） | SQL |
| 4 | `unschedule` | 函数 | `(text)` | boolean | 删除任务（按名称） | SQL |
| 5 | `alter_job` | 函数 | `(bigint, boolean, text, text)` | void | 修改任务配置 | SQL |
| 6 | `run_job` | 函数 | `(bigint)` | bigint | 立即执行任务 | SQL |

### 5.2 分区管理函数 (pg_partman兼容)

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_parent` | 函数 | `(text, text, text, text, integer, text)` | boolean | 创建分区配置 | SQL |
| 2 | `run_maintenance` | 函数 | `(text, boolean)` | integer | 运行分区维护 | SQL |
| 3 | `create_partitions` | 函数 | `(text)` | integer | 创建分区 | SQL |
| 4 | `drop_old_partitions` | 函数 | `(text)` | integer | 删除旧分区 | SQL |
| 5 | `set_retention` | 函数 | `(text, text, boolean)` | void | 设置保留策略 | SQL |

### 5.3 任务依赖链函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `add_dependency` | 函数 | `(bigint, bigint)` | boolean | 添加任务依赖 | SQL |
| 2 | `remove_dependency` | 函数 | `(bigint, bigint)` | boolean | 移除任务依赖 | SQL |
| 3 | `get_dependencies` | 函数 | `(bigint)` | table | 获取依赖链 | SQL |
| 4 | `check_dependencies_met` | 函数 | `(bigint)` | boolean | 检查依赖是否满足 | SQL |

### 5.4 任务重试机制函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `set_retry_config` | 函数 | `(bigint, integer, integer)` | boolean | 设置重试配置 | SQL |
| 2 | `run_job_with_retry` | 函数 | `(bigint)` | table | 带重试执行任务 | SQL |

### 5.5 任务组管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_job_group` | 函数 | `(text, text)` | bigint | 创建任务组 | SQL |
| 2 | `add_job_to_group` | 函数 | `(text, bigint, integer)` | boolean | 添加任务到组 | SQL |
| 3 | `run_job_group` | 函数 | `(text)` | table | 运行任务组 | SQL |

### 5.6 Cron解析函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `parse_cron` | 函数 | `(text)` | table | 解析cron表达式 | SQL |
| 2 | `validate_cron` | 函数 | `(text)` | boolean | 验证cron表达式 | SQL |
| 3 | `cron_matches_now` | 函数 | `(text)` | boolean | 检查是否匹配当前时间 | SQL |
| 4 | `get_next_run_time` | 函数 | `(text, timestamp)` | timestamp | 获取下次执行时间 | SQL |

### 5.7 预设任务模板

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `schedule_vacuum` | 函数 | `(text, text)` | bigint | 创建定时VACUUM任务 | SQL |
| 2 | `schedule_analyze` | 函数 | `(text, text)` | bigint | 创建定时ANALYZE任务 | SQL |
| 3 | `schedule_cleanup_logs` | 函数 | `(integer, text)` | bigint | 创建定时清理日志任务 | SQL |
| 4 | `version` | 函数 | `()` | text | 版本信息 | SQL |

### 5.8 视图

| 序号 | 名称 | 类型 | Schema | 功能说明 |
|------|------|------|--------|----------|
| 1 | `job` | 视图 | otb_scheduler | 任务列表视图 |
| 2 | `job_run_details_view` | 视图 | otb_scheduler | 执行历史视图 |
| 3 | `job_stats` | 视图 | otb_scheduler | 任务统计视图 |
| 4 | `partition_stats` | 视图 | otb_scheduler | 分区统计视图 |

### 5.9 自定义类型

| 序号 | 名称 | 类型 | 值 | 功能说明 |
|------|------|------|-----|----------|
| 1 | `job_status` | 枚举 | active, inactive, running, failed | 任务状态枚举 |

### 5.10 元数据表

| 序号 | 名称 | Schema | 功能说明 |
|------|------|--------|----------|
| 1 | `jobs` | otb_scheduler | 定时任务表 |
| 2 | `job_run_details` | otb_scheduler | 任务执行日志 |
| 3 | `part_config` | otb_scheduler | 分区配置表 |
| 4 | `part_log` | otb_scheduler | 分区日志 |
| 5 | `job_dependencies` | otb_scheduler | 任务依赖表 |
| 6 | `job_groups` | otb_scheduler | 任务组表 |
| 7 | `job_group_members` | otb_scheduler | 任务组成员表 |

---

## 6. 时序分析算法库 (otb_analytics) - 原创

### 6.1 移动平均算法族

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `sma` | 聚合 | `(float8, int)` | float8 | 简单移动平均 | C |
| 2 | `ema` | 聚合 | `(float8, float8)` | float8 | 指数移动平均 | C |
| 3 | `wma` | 聚合 | `(float8, int)` | float8 | 加权移动平均 | C |
| 4 | `dema` | 聚合 | `(float8, float8)` | float8 | 双指数移动平均 | C |
| 5 | `tema` | 聚合 | `(float8, float8)` | float8 | 三指数移动平均 | C |

### 6.2 异常检测算法

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `detect_anomalies_zscore` | 聚合 | `(float8, float8)` | int | Z-score异常检测 | C |
| 2 | `detect_anomalies_iqr` | 聚合 | `(float8, float8)` | int | IQR异常检测 | C |

### 6.3 高级聚合函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `delta` | 聚合 | `(float8)` | float8 | 差值计算 | C |
| 2 | `cumsum` | 聚合 | `(float8)` | float8 | 累积和 | C |
| 3 | `rate` | 聚合 | `(float8, int8)` | float8 | 变化率 | C |

### 6.4 版本函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `version` | 函数 | `()` | text | 版本信息 | SQL |

---

## 7. 数据健康诊断 (otb_health) - 原创

### 7.1 数据质量检查

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `check_time_gaps` | 函数 | `(regclass, text, interval, interval)` | table | 时间间隙检测 | SQL |
| 2 | `check_duplicates` | 函数 | `(regclass, text)` | table | 重复数据检测 | SQL |
| 3 | `check_nulls` | 函数 | `(regclass)` | table | 空值分析 | SQL |

### 7.2 智能诊断

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `health_check` | 函数 | `(regclass, text)` | table | 综合健康检查（6项） | SQL |
| 2 | `auto_tune_advisor` | 函数 | `(regclass)` | table | 自动调优建议 | SQL |

### 7.3 分区诊断

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `explain_pruning` | 函数 | `(regclass, timestamp, timestamp)` | table | 分区裁剪分析 | SQL |
| 2 | `verify_indexes` | 函数 | `(regclass)` | table | 索引完整性验证 | SQL |
| 3 | `recommend_partition_strategy` | 函数 | `(regclass, int)` | table | 分区策略推荐 | SQL |

### 7.4 版本函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `version` | 函数 | `()` | text | 版本信息 | SQL |

---

## 8. 数据快照系统 (otb_snapshot) - 原创

### 8.1 快照管理函数

| 序号 | 名称 | 类型 | 参数 | 返回值 | 功能说明 | 实现 |
|------|------|------|------|--------|----------|------|
| 1 | `create_snapshot` | 函数 | `(regclass, text, text)` | text | 创建数据快照 | SQL |
| 2 | `list_snapshots` | 函数 | `(regclass)` | table | 列出所有快照 | SQL |
| 3 | `rollback_to_snapshot` | 函数 | `(regclass, text, boolean)` | text | 回滚到快照 | SQL |
| 4 | `drop_snapshot` | 函数 | `(regclass, text)` | text | 删除快照 | SQL |
| 5 | `version` | 函数 | `()` | text | 版本信息 | SQL |

### 8.2 元数据表

| 序号 | 名称 | Schema | 功能说明 |
|------|------|--------|----------|
| 1 | `snapshots` | otb_snapshot | 快照元数据表 |

---

## 📋 附录：公共Schema别名函数

以下函数在 `public` schema 下创建了便捷别名，可直接调用：

### A.1 时序分析 (来自 otb_analytics)
- `sma(float8, int)`, `ema(float8, float8)`, `wma(float8, int)`, `dema(float8, float8)`, `tema(float8, float8)`
- `detect_anomalies_zscore(float8, float8)`, `detect_anomalies_iqr(float8, float8)`
- `delta(float8)`, `cumsum(float8)`, `rate(float8, int8)`

### A.2 数据健康 (来自 otb_health)
- `check_time_gaps(regclass, text, interval, interval)`
- `check_duplicates(regclass, text)`
- `check_nulls(regclass)`
- `health_check(regclass, text)`
- `auto_tune_advisor(regclass)`

### A.3 数据快照 (来自 otb_snapshot)
- `create_snapshot(regclass, text, text)`
- `list_snapshots(regclass)`
- `rollback_to_snapshot(regclass, text, boolean)`
- `drop_snapshot(regclass, text)`

### A.4 全文检索 (来自 otb_fulltext)
- `ft_tokenize(text)`, `ft_match(text, text)`, `ft_rank(text, text)`, `ft_highlight(text, text)`

### A.5 图操作 (来自 otb_age)
- `create_graph(text)`, `drop_graph(text, boolean)`

---

## 🔗 相关文档

| 文档 | 说明 |
|------|------|
| [README.md](../README.md) | 项目概述与使用指南 |
| [技术报告.pdf](../技术报告.pdf) |   技术报告  |
| [技术详析.md](./技术详析.md) |    详细技术实现    |
| [商业价值分析.md](./商业价值分析.md) | 商业价值分析 |

---

*最后更新：2025年12月7日*

